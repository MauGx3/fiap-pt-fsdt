# Copy this file to .env and adjust the values before running Docker Compose.
# These variables are consumed by docker compose when orchestrating the stack.

# Required for signing JWT access tokens. Use a strong, random value.
JWT_SECRET=please-generate-a-strong-secret

# Optional: change the host ports exposed by docker compose.
BACKEND_PORT=3000
FRONTEND_PORT=8080
